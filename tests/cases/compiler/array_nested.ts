// @reference: bootstrap/array-nested
type Matrix = number[][];
type Grid = { rows: string[][] };

const matrix: Matrix = [[1, 2], [3], []];
const direct: boolean[][][] = [[[true]], [[false, true]]];
const grid: Grid = { rows: [["a", "b"], ["c"]] };
