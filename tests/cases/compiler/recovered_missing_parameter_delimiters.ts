// @reference bootstrap/editor-recovery/missing-parameter-delimiters
function format(value: number suffix: string): string {
  return suffix;
}
const result: string = format(1, "ok");
const intact: number = "wrong";
