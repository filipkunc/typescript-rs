// @reference: bootstrap/diagnostic-assignment-anchors
type Config = { enabled: boolean; counts: number[] };

const primitive: string = 1;
const config: Config = { enabled: "yes", counts: [1, "two"] };
