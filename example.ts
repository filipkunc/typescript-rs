const project: string = "tsrs";

// Deliberately invalid: F5 should stop in `Checker::check_variable_declarator`
// when a breakpoint is placed on the diagnostic branch in src/checker.rs.
const version: string = 1;
